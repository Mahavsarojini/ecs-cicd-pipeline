pipeline {
  agent any
  stages {
    stage('Checkout') {
      steps {
        git 'https://github.com/<username>/ecs-cicd-pipeline.git'
      }
    }
    stage('Build Docker Image') {
      steps {
        sh 'docker build -t my-app:${BUILD_NUMBER} .'
      }
    }
  }
}
